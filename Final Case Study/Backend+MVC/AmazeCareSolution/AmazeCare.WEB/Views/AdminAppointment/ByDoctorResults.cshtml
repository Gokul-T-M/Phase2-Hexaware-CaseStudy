@model IEnumerable<AmazeCare.WEB.Models.AdminAppointment.AppointmentResponseViewModel>

@{
    ViewBag.Title = $"Appointments for Doctor ID {ViewBag.DoctorId}";
}

<h2>Appointments for Doctor ID @ViewBag.DoctorId</h2>

<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>Date</th>
            <th>Patient</th>
            <th>Symptoms</th>
            <th>Status</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var appt in Model)
        {
            <tr>
                <td>@appt.AppointmentDate.ToString("g")</td>
                <td>@appt.PatientName</td>
                <td>@appt.Symptoms</td>
                <td>@appt.Status</td>
                <td>
                    <a asp-action="Details" asp-route-id="@appt.AppointmentId" class="btn btn-info btn-sm">Details</a>
                </td>
            </tr>
        }
    </tbody>
</table>

<a asp-action="ByDoctor" class="btn btn-secondary">Back</a>

